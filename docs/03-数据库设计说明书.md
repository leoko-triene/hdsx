# 数据库设计说明书

## 2026-07 表结构增补

- `knowledge_point_relations`：保存课程知识点之间的前置、相关、包含关系及置信度。
- `knowledge_point_graphs`：保存每次生成的知识图谱版本快照（nodes/edges JSON）、状态与审核信息。
- `lesson_resources.is_saved`：区分教师主动保存与最多 30 条临时生成历史。
- `questions.material_type`：标记课堂例题、练习题、思考题或拓展材料。
- `qa_messages.original_content/correction_note/corrected_by/corrected_at`：保留 AI 原回答及教师修正证据。
- `notifications`：按用户存储作业截止、答疑修正等站内通知，以 `notification_key` 保证幂等。
- 家长学生关系继续由 `parent_student_links` 隔离，课程操作由 `course_managers` 校验。
- `documents.source_url`：保存确认入库网页的最终来源地址，本地上传文件保持为空。
- `web_import_drafts`：保存网页正文预览、来源、内容哈希、确认状态和 24 小时有效期；迁移版本为 `0008_web_imports`。

## 1. 概述

业务数据库采用官方 MySQL 8.4 Docker 镜像，当前实际服务版本 8.4.10，字符集 `utf8mb4`；数据持久化在 `D:\Docker\mysql`。向量库采用现有 Milvus。MySQL 保存权威业务数据、文档块正文和检索元数据，Milvus 仅保存向量及 `chunk_id`。文件本体保存于 `storage/`。

## 2. 配置与迁移

数据库名默认为 `ai_education`，应用账号为 `ai_education_app`。管理员先执行 `database/init_mysql.sql` 并修改密码，再配置 `.env`。结构通过 `backend/migrations` 管理：

```powershell
conda activate program-hd
cd backend
alembic upgrade head
```

生产环境禁止应用使用 root。密码应通过环境变量注入。连接启用 `pool_pre_ping`、1800 秒 recycle、默认池 10、溢出 20。

## 3. 逻辑关系

```text
User 1---N Course 1---N Chapter 1---N KnowledgePoint
                 |            1---N KnowledgePointRelation
                 |            1---N KnowledgePointGraph
                 |
                 +---N Document 1---N DocumentChunk ---1 Milvus Vector
                 +---N WebImportDraft ---0..1 Confirmed Document
                 +---N LessonResource
                 +---N Assignment 1---N Question
                                      |
User(Student) 1---N Submission -------+---N GradingResult
User ---------N QASession 1---N QAMessage
User(Student) 1---N MasterySnapshot
User(Student) 1---N LearningPath
User(Student) 1---N Report N---1 Course
```

## 4. 表说明

| 表 | 主键/唯一约束 | 主要用途 |
| --- | --- | --- |
| users | id；username/public_id 唯一 | 用户和单一主角色 MVP |
| parent_student_links | id；parent_id+student_id 唯一 | 家长学生授权关系 |
| courses | id；public_id 唯一 | 课程及负责人 |
| course_managers | id；course_id+user_id 唯一 | 课程多负责人关系 |
| course_members | id；course_id+student_id 唯一 | 已审批/导入的课程学生成员 |
| course_join_requests | id；course_id+student_id 唯一 | 学生加入申请、状态与审批意见 |
| classes | id；course_id+name 唯一 | 教学班 |
| class_members | id；class_id+user_id 唯一 | 班级成员 |
| chapters | id | 树形章节 |
| knowledge_points | id；course_id+code 唯一 | 课程知识点 |
| knowledge_point_relations | id；from_kp_id+to_kp_id+relation_type 唯一 | 知识点关系（前置/相关/包含） |
| knowledge_point_graphs | id；course_id+version 索引 | 知识图谱版本快照 |
| documents | id；file_hash 索引 | 上传文档与状态 |
| web_import_drafts | id；course/status/content_hash 索引 | 网页抓取待确认正文、来源与确认结果 |
| document_chunks | id；document_id+chunk_index 唯一 | 分块正文和元数据 |
| lesson_resources | id | 教案、讲稿、PPT 大纲和练习 |
| assignments | id | 作业主记录 |
| questions | id | 题目、答案、rubric、关联知识点 ID |
| submissions | id；assignment_id+student_id 唯一 | 学生提交 |
| grading_results | id；submission_id+question_id 唯一 | 规则/AI/最终评分 |
| qa_sessions | id | 课程问答会话 |
| qa_messages | id；trace_id 索引 | 问答、引用和置信度 |
| mastery_snapshots | id | 知识点掌握度历史 |
| learning_paths | id | 个性化任务 JSON |
| reports | id | 周/月报、指标、内容和审核状态 |
| async_tasks | id；task_id 唯一 | 后续异步任务状态 |
| audit_logs | id；action/created_at 索引 | 关键操作审计 |

## 5. 关键字段与类型

- 所有主键使用 `BIGINT AUTO_INCREMENT`。
- 外部标识 `public_id` 使用 UUID 字符串，避免暴露连续主键。
- 成绩使用 `DECIMAL(7,2)`，模型置信度使用 FLOAT 且代码限制在 0～1。
- 文档正文、题干和评语使用 TEXT；结构化内容使用 JSON。
- 网页草稿正文使用 MySQL MEDIUMTEXT，应用层限制为 5 万字符；来源 URL 使用 VARCHAR(2000)。
- `lesson_resources.content_json` 继续使用 JSON 列兼容历史版本，新生成内容以 `{title, text, format, resource_type}` 保存，其中 `text` 为可直接阅读和下载的 Markdown 正文。
- 状态使用统一枚举：draft、processing、ready、published、pending_review、approved、failed、archived。
- 时间统一由服务端产生并以 UTC 语义保存。

## 6. 索引策略

- 用户名、课程 owner、文档 course/category、提交 student/assignment、掌握度 student/knowledge_point 建普通索引。
- 文档块对 `content` 建 MySQL FULLTEXT，并保留 LIKE 兜底。
- 使用 `content_hash` 防止块级重复；文档使用 `(course_id, dedup_key)` 唯一约束阻止并发上传相同内容。历史文档的 `dedup_key` 可为空，新上传文件均写入 SHA-256。
- 网页确认时以提取后的正文 SHA-256 同时写入 `file_hash` 和 `dedup_key`，防止相同正文通过不同 URL 重复入库。
- 分页查询统一使用稳定排序；大表后期采用游标分页。

## 7. 事务与一致性

- 文档上传先保存文件和 MySQL 状态，再处理块；异常回滚数据库并保留可诊断文件。
- 网页抓取只写 `web_import_drafts`；确认事务成功后关联 `confirmed_document_id`。拒绝、过期或索引失败的草稿不得成为 ready 文档。
- Milvus 不参与 MySQL 事务。正式向量入库采用状态机和幂等补偿：MySQL processing → 写向量 → 回写 milvus_id → ready。
- 批改结果和提交总分在同一事务写入；教师复核不得覆盖 AI 建议分。
- 报告指标和自然语言版本一起保存，防止内容与后续实时数据不一致。

## 8. Milvus 设计

Collection 默认 `edu_chunks_dev`，向量维度 768（已从当前 embeddinggemma 环境确认）。字段包含主键、chunk_id、course_id、document_id、category、content_hash、embedding 和 created_at。查询必须带 course_id 权限过滤，返回 chunk_id 后回查 MySQL。

## 9. 备份与安全

定期执行 MySQL 逻辑备份和 storage 文件备份，并保持同一恢复点。备份不提交 Git。恢复测试应验证 MySQL 文档块与 Milvus chunk_id 一致性。审计日志和成绩数据设置更长保留期。

## 10. 0010～0011 新增字段与图谱语义

- `0010_question_explanation`：为 `questions` 增加 `explanation`（Text，nullable），保存题目解析。
- `0011_knowledge_graph`：新增 `knowledge_point_relations` 和 `knowledge_point_graphs` 两张表，均带 `course_id`、`created_at`、`updated_at` 外键/索引；关系表唯一键为 `(from_kp_id, to_kp_id, relation_type)`，图谱表按 `(course_id, version)` 建索引。当前 Alembic head 为 `0011_knowledge_graph`。

## 11. 0009 重复提交与删除语义

`submissions` 新增非空 `attempt_no`，唯一键调整为 `(assignment_id, student_id, attempt_no)`，并增加最新尝试复合索引。课程和作业采用 `archived` 归档删除以保留成绩审计链；知识库文档按负责人确认执行实体删除，先清理向量和 `document_chunks`，再删除文件记录与受控 storage 文件。

## 12. `plugins` 协作模块数据约束

`plugins/` 模块原则上不直接持有 SQLAlchemy Session，不得调用 `create_all`、执行任意 DDL 或维护 SQLite/第二套迁移链。`plugins/spider` 只返回搜索与正文结果，知识库写入继续由 `WebImportService` 负责；答疑网络补充不落库。需要新增表或字段时必须进入主项目 Alembic 流程。本轮数据库版本仍为 `0009_submission_attempts`。
# 2026-07-15 知识图谱数据补充

新增 `knowledge_point_relations` 和 `knowledge_point_graphs` 表，由 Alembic `0011_knowledge_graph` 管理。知识点按 `course_id+code` 去重，关系按 `from_kp_id+to_kp_id+relation_type` 去重，每次生成创建新的 `knowledge_point_graphs` 版本记录；失败时整体回滚，不会留下空图谱。学生掌握度继续复用 `mastery_snapshots`，不重复计算多次提交。

# 2026-07-14 数据存储补充

本轮不修改 MySQL 表结构，Alembic head 保持 `0009_submission_attempts`。知识库预览由现有 `documents` 与按 `chunk_index` 排序的 `document_chunks` 重组，不保存重复预览副本。管理员模型配置属于部署级秘密，不进入业务数据库：完整 JSON 使用 `SECRET_KEY` 派生的 Fernet 密钥加密后保存为 `storage/system/model-providers.enc`，该路径已被 `.gitignore` 排除。

Embedding 配置包含维度和 Milvus collection。已有 collection 的向量字段维度必须与当前配置一致，否则拒绝使用；更换向量空间后应创建新 collection 并重建既有文档向量。
# 2026-07-14 数据读取补充

本轮不修改表结构，Alembic head 保持 `0009_submission_attempts`。课程学情通过现有 `assignments`、`submissions`、`qa_sessions`、`qa_messages`、`mastery_snapshots` 和 `knowledge_points` 只读聚合：未提交作业通过“已发布作业全集减去学生最新提交”得到，重复提交只取最大 `attempt_no`，知识点掌握度通过 `knowledge_points.course_id` 按课程隔离。

# 2026-07-15 知识图谱读取补充

知识点图谱查询使用 `knowledge_point_graphs` 最新版本；知识点列表直接查询 `knowledge_points`。学生掌握度按课程取 `mastery_snapshots` 每个知识点的最新记录，并结合该课程已发布题目中各知识点的出现次数。所有读取接口均校验课程访问权限。
