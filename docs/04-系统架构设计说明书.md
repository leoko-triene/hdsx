# 系统架构设计说明书

## 2026-07 教学闭环增补

Vue 使用 `KeepAlive` 保存页面实例，答疑会话同时落入 MySQL，实现切换与重新进入后的双层恢复。教师课堂运营聚合 `qa_messages` 与低分 `grading_results`，修正回答后写入通知；学生读取通知时由幂等规则补充 48 小时内未提交作业提醒。作业材料生成仅接收有权课程的已就绪知识库文件，输出经结构校验后写入题目表。

备课 Agent 输出 Markdown 文本，并按教案、讲稿、PPT 提纲和练习使用不同格式约束；数据库以 JSON 包装保存文本以兼容旧记录。作业材料 Agent 优先使用 Ollama JSON Schema 结构化输出，再经过代码块解析、字段别名归一化和受控文本降级。前端顶栏使用路由元数据计算当前位置，并以 sticky 布局固定账号与退出入口。

备课证据路由先执行 MySQL 整句匹配，未命中时运行 Milvus 向量召回、RRF 与 BGE 重排序，仅接受 `rerank_score > 0` 的候选；禁止退回无主题约束的最近文档块。多文件作业生成在 API 层校验全部文件属于当前课程且为 ready，并在 Service 层按文件均衡抽取文档块后合并上下文。

## 2026-07 Agent 平台结构补强

```text
backend/app/
├─ common/          # AgentResult、调用元数据、TraceContext
├─ agents/          # Runtime 与备课/作业/批改/答疑/报告/知识图谱领域 Agent
├─ prompts/         # *.prompt.json 版本化模板与注册中心
├─ skills/          # manifest.json + SKILL.md 工作流契约与注册中心
├─ tools/           # 课程权限保护的只读 Tool 与白名单注册中心
├─ mcp/             # 站内 MCP JSON-RPC 安全网关
├─ integrations/mcp # 默认关闭、主机白名单限制的远端客户端
└─ rag/             # loader/cleaner/splitter/embedding/index/retrieval/reranker/context/citation/pipeline
```

调用链为 `API/Service → Domain Agent → Skill Registry → Prompt Registry → Ollama`。知识检索等工具由业务层确定性执行，Runtime 校验所记录的工具是否属于 Skill 白名单；这避免让模型任意选择工具或直接接触 Session。MCP 的 `tools/call` 仍经过同一 Tool Registry、参数校验和课程权限。知识图谱抽取同样通过 `KnowledgeGraphAgent` 运行 `knowledge_graph_extraction@1.0.0` Skill 和对应版本化 Prompt。

Prompt 文件声明名称、版本、变量、系统模板、用户模板和 text/json 输出模式；启动/测试时注册中心检查重复版本、缺失变量和未声明占位符。Skill 文件声明 Prompt 版本、允许 Tool、输入输出 Schema 和不可变工作流说明。新增或升级能力不得原地覆盖旧版本。

内部 MCP 当前实现经过认证的 JSON-RPC 子集：`initialize`、`ping`、`tools/list`、`tools/call`，支持 `2025-11-25` 与 `2025-06-18` 版本协商。它不是匿名公网 MCP 服务；远端客户端默认关闭，只有 `MCP_REMOTE_ENABLED=true` 且目标主机存在于 `MCP_ALLOWED_HOSTS` 时才允许连接。

## 2026-07 网络资料架构

```text
教师关键词/URL
  → WebArticleCrawler 适配层 → plugins/spider（DNS/IP/端口/逐跳重定向/大小校验）
  → 正文提取与清洗
  → WebImportDraft(pending，24h)
  → 前端人工预览
  ├─ 拒绝/过期：不进入知识库
  └─ 确认：Document(source_url) → 分块 → Embedding → Milvus → ready
       → 备课/作业/答疑/标准答案的原有 RAG
```

`preview_web_source` 作为 MCP Tool 注册，允许创建待确认草稿，因此标记为非只读；确认接口没有注册成 Tool，模型无法替用户批准。`search_web_knowledge` 是答疑只读 Tool。抓取器不执行 JavaScript、不携带站点登录态、不访问非文本资源，也不尝试绕过反爬或登录。网页正文被视为不可信输入，活跃 Prompt `1.1.0` 明确禁止执行资料内命令。

## 1. 架构目标

系统采用模块化单体，优先保证本地可运行、边界清晰和 Agent 可持续维护。FastAPI 承载业务接口，Worker 作为后续独立进程处理耗时任务。该结构可在需要时按知识库、批改和报告模块拆分服务。

## 2. 分层架构

```text
[Vue 3 Web]
     |
[FastAPI API：校验、鉴权、序列化]
     |
[Domain Service：业务规则、事务、状态机]
     |
[Repository/ORM] ---- [Agent Runtime/Tools/Skills]
     |                          |
 [MySQL]             [RAG/Ollama/Reranker/Milvus]
```

API 不直接操作 ORM 或拼 Prompt；Service 不依赖前端；Agent 通过白名单工具读取业务数据，不能直接获取数据库连接。

## 3. 代码结构

- `backend/app/api`：REST 路由、Schema 和鉴权依赖。
- `backend/app/core`：配置、安全、异常。
- `backend/app/db`：SQLAlchemy Base、Engine 和 Session。
- `backend/app/modules`：领域 ORM；后续按 identity/course/knowledge 等拆包。
- `backend/app/rag`：加载、清洗、分块、向量化、索引、混合召回、融合去重、重排、上下文和引用流水线。
- `backend/app/agents`、`prompts`、`skills`、`tools`：领域 Agent、版本化提示词、工作流契约和安全工具。
- `backend/app/common`、`mcp`：共享调用契约、追踪和 MCP 安全网关。
- `backend/app/integrations`：Ollama、Milvus、重排序等外部适配。
- `backend/app/services`：文档解析、备课和批改编排。
- `frontend/src`：路由、状态、API 客户端、公共组件、composable、类型和业务页面；管理员能力中心展示注册状态。
- `database`、`scripts`、`docs`、`storage`：数据库初始化、运维、文档和文件数据。

## 4. RAG 架构

### 4.1 入库

文件校验 → TXT/MD/PDF/DOCX 解析 → 清洗 → 递归分块（650/100）→ 必需元数据 → embeddinggemma → Milvus → MySQL FULLTEXT → 抽样验证。

当前代码已实现解析、分块、元数据、Ollama Embedding、Milvus 写入和向量召回、MySQL 检索、RRF 融合与 BGE 重排，并已连接真实服务完成验证。为避免 8GB 显存与 Ollama 7.6B 竞争，BGE 默认在 CPU 运行。

### 4.2 查询

权限和课程过滤 → 查询改写 → 向量/关键词双路召回 → 基于 chunk_id/content_hash 去重 → RRF 融合 → BGE 重排 → 上下文构建 → Qwen 生成 → 引用检查 → 返回或转教师。

## 5. Agent 架构

业务 Agent 包括备课、批改、答疑、学情、报告和知识图谱。统一结果语义为 success、needs_review、insufficient_evidence、failed，并包含 data、citations、confidence、warnings 和 trace_id。

工具只提供受控能力，例如 `search_course_knowledge`、`get_assignment_rubric`、`get_student_mastery_summary`。任何写操作经 Service 权限、状态和幂等校验。Skill 固化稳定工作流，Prompt 使用版本号和 JSON Schema。

## 6. 关键业务时序

### 答疑

```text
Student -> API: question
API -> Auth: course permission
API -> RAG: search(course_id, question)
RAG -> MySQL/Milvus: retrieve
RAG -> Reranker: rerank
RAG -> plugins/spider: if evidence < 2, fetch at most 2 public pages
RAG -> Ollama: grounded prompt
Ollama -> API: answer + course/web citations
API -> MySQL: save message/citations/trace
API -> Student: answer or needs_teacher
```

### 批改

```text
Teacher -> API: grade submission
API -> RuleEngine: objective questions
API -> GradingAgent: subjective answer + rubric
GradingAgent -> Ollama: structured grading
API -> MySQL: save suggestion and evidence
Teacher -> API: final review
API -> MySQL: final score + audit + mastery event
```

### 知识图谱

```text
Teacher -> API: generate knowledge graph(course_id, optional document_ids)
API -> Service: owned_course check
Service -> RAG: read ready document chunks
Service -> KnowledgeGraphAgent: extract knowledge_points + relations
KnowledgeGraphAgent -> Ollama: structured extraction
Service -> MySQL: upsert knowledge_points, replace relations, create graph version
Teacher -> API: approve graph
API -> MySQL: status approved, reviewed_by, reviewed_at
```

## 7. 安全架构

JWT、PBKDF2 密码、RBAC、资源归属共同保护接口。上传文件使用安全文件名和大小限制。文档内容作为不可信数据，不能覆盖系统 Prompt。模型输出需要 JSON/范围校验。CORS 仅允许本地前端地址，生产部署必须改为真实域名。

## 8. 可用性与扩展

- 健康接口区分进程存活和依赖就绪。
- 外部依赖错误转为结构化 503。
- 耗时调用设置 120 秒超时，后续通过 Celery/Redis 异步化。
- MySQL、Milvus、Ollama 均通过适配层，可替换部署地址。
- 文件存储适配层后续可由本地目录切换 MinIO。

## 9. 部署拓扑

开发环境在 Windows 运行 FastAPI、Vue 和 Ollama；MySQL 使用独立 `edu-mysql` 容器并绑定本机 3306，Milvus 使用已有 Docker。Linux 部署建议由 Nginx 提供 HTTPS 和静态文件，后端用 Uvicorn/Gunicorn，MySQL 和 Milvus 使用持久卷，模型服务部署在带 GPU 的主机。

当前项目提供 `start_production.ps1`：先执行 Vue 类型检查和生产构建，再由 FastAPI 同源托管 SPA 与 `/api/v1`，单端口 8000、不启用 reload。公网环境需要在其前方增加 HTTPS 反向代理、正式密钥、访问日志、备份与防火墙。

## 10. 结构化输出契约与分层导航

Agent Runtime 将完整 JSON Schema 传给 Ollama；服务层再用 Pydantic 验证并归一化字段。若模型输出无法解析，批改、材料和报告分别由确定性工厂组装合法对象，接口仍返回稳定契约并标记低置信度/需复核。前端模块入口与课程详情使用独立路由，面包屑由路由参数组合模块、课程和作业层级；桌面侧栏使用 sticky 视口定位。

## 11. 源码组合模块、启动预热与前端布局

协作者实现位于根目录 `plugins/`，通过 `__init__.py` 只导出稳定公共接口；`backend/app/integrations` 负责异步与错误契约适配，Service 负责权限、事务和业务编排。当前 `plugins/spider` 提供实际搜索与抓取，知识库负责确认入库，答疑只做临时只读补充。系统不进行运行时插件发现或动态页面挂载。

FastAPI lifespan 在接受流量前调用模型预热服务：Ollama 对话模型与 Embedding 模型依次加载并使用 `keep_alive=-1`，配置了本地 BGE 时同步预载到指定设备。预热不下载模型，结果写入 `/health/ready`；严格模式可阻止失败实例启动。前端知识库在标题下使用横向滚动课程标签，工作区为左侧上传/爬虫、右侧检索/文件；Markdown 渲染继续先转义原始 HTML。
# 2026-07-14 架构增补：模型 Provider

`plugins/model_providers` 是源码级组合模块：`contracts.py` 定义稳定配置及 LLM/Embedding 抽象，`providers.py` 实现 Ollama 与 OpenAI-compatible 协议，`store.py` 负责加密配置文件。核心层 `integrations/model_runtime.py` 承担默认值、业务校验和异常转换，原 `OllamaClient` 保留兼容外观，使 Agent Runtime、RAG、备课、作业和批改统一动态转发。

```text
管理员页面 → admin-only API → ModelRuntimeManager → plugins/model_providers
                                               ├→ Ollama
                                               └→ OpenAI-compatible API
Agent/Skill/Prompt → 兼容外观 ──────────────────┘
RAG → Embedding Provider → 维度校验 → Milvus collection
```

知识库预览读取 MySQL 已解析分块并按序重组，不暴露服务器文件路径；前端 Markdown 渲染器先转义 HTML，并限制链接协议。
# 2026-07-14 路由缓存与分析架构增补

Vue 根路由视图以 `route.fullPath` 作为 `KeepAlive` key，使相同 View 的不同 `courseId` 各自保留状态而不串课。作业详情导航先切换路由，再由详情路由实例加载数据，防止污染课程作业列表缓存。课程上下文条由根布局统一生成，课程名来自课程卡片路由查询，返回地址来自模块路径。

RAG Pipeline 依次执行 RRF、去重、BGE 重排、正证据过滤和来源多样性过滤；未配置重排模型时以 `None` 表示“未重排”，继续使用融合排序。课程学情的数据查询留在 API 权限边界，纯计算由 `services/analytics.py` 完成，便于独立测试。
