# 测试用例

## 2026-07 新增验收用例

1. 知识点图谱仅课程负责人/管理员可生成、查看和审核；学生只能查看本人掌握度，家长只能查看已绑定学生。
2. 未加入课程的学生无法读取该课程知识点列表或图谱。
3. 模型返回空知识点或非法 JSON 时，不创建空图谱，返回明确业务错误。
4. 多次提交同一作业只按最新 `MasterySnapshot` 统计掌握度，不重复计数。
5. 重新生成图谱时版本号递增，旧版本保留可查。
6. 题目关联知识点后，学生掌握度计算包含该知识点。
7. 页面切换后课程、输入和对话不丢失；刷新后可恢复答疑历史。
2. 未保存备课超过 30 条时仅删除最久临时记录，已保存记录不受影响。
3. 家长不可读取未绑定学生；教师不可审批或操作非负责课程。
4. 相同内容不同文件名仍被知识库 SHA-256 去重阻止。
5. 作业按指定知识库文件和章节生成四类材料，学生接口不泄露答案。
6. 薄弱画像仅展示授权学生，推荐内容来自对应课程知识库。
7. 教师修正回答后学生产生未读通知，重新进入对话看到修正标记。
8. 48 小时内到期且未提交的作业只生成一条幂等通知。
9. 家长报告按绑定学生切换，文案不包含难懂的内部字段或原始 JSON。
10. 材料生成接受 Markdown JSON 代码块、嵌入文本和顶层数组。
11. 多选答案顺序不影响判分，少选、多选与错选均不能得满分。
12. 判断题在出题端和学生端均显示正确/错误两个互斥选项。
13. 学生提交后同一请求完成批改，结果接口返回标准答案；主观题结合知识库判分。
14. 报告列表字段为字符串时按完整语句渲染，不得逐字换行。

## 1. 测试策略

测试分为单元、API 集成、外部服务集成、前端和端到端五层。自动化测试默认使用独立 MySQL 测试库；不得以 SQLite 替代 MySQL 行为。Ollama/Milvus 在普通单元测试中使用 mock，在标记的真实集成测试中连接本地服务。

## 2. 当前自动化用例

| 编号 | 用例 | 预期 |
| --- | --- | --- |
| UT-KG-01 | `KnowledgeGraphAgent` 声明 Skill 与版本 | `knowledge_graph_extraction@1.0.0` |
| UT-KG-02 | 空知识点抽取拒绝生成 | 抛出 `KG_EXTRACTION_EMPTY` 业务错误 |
| UT-KG-03 | 掌握度复用 `MasterySnapshot` | 返回最新快照并按课程知识点聚合 |
| AGENT-KG-01 | Skill/Prompt 注册解析 | `knowledge_graph_extraction` 与对应 Prompt 版本存在 |
| UT-001 | PBKDF2 密码生成和校验 | 正确密码通过，错误密码失败 |
| UT-002 | JWT 创建和解析 | sub、role 正确且签名有效 |
| UT-003 | 递归分块 | 产生多块，索引连续，元数据完整 |
| UT-004 | 非法分块参数 | overlap>=size 抛出异常 |
| API-001 | `/health/live` | 无外部依赖也返回 200 |
| AGENT-001 | Skill 与 Prompt 注册解析 | 每个 Skill 的指定 Prompt 版本均存在 |
| AGENT-002 | Skill 请求未声明 Tool | Runtime 在模型调用前拒绝 |
| TOOL-001 | Tool 注册中心 | 5 个只读 Tool 和 1 个只生成待确认草稿的网页预览 Tool |
| MCP-001 | MCP `tools/list` | 只返回 Tool Registry 的注册项 |
| RAG-001 | 分块去重与上下文构建 | 去重稳定且来源含文件名、chunk_id |

执行：`conda run -n program-hd python -m pytest`。

## 3. 身份与权限测试

| 编号 | 前置 | 步骤 | 预期 |
| --- | --- | --- | --- |
| AUTH-01 | 空数据库 | bootstrap 创建 admin | 201/200，用户密码为哈希 |
| AUTH-02 | 已有用户 | 再次 bootstrap | 409 BOOTSTRAP_CLOSED |
| AUTH-03 | 有效账号 | 登录 | 返回 bearer token |
| AUTH-04 | 错误密码 | 登录 | 401 INVALID_CREDENTIALS |
| AUTH-05 | student token | 创建课程 | 403 PERMISSION_DENIED |
| AUTH-06 | 教师 A/B | B 操作 A 的课程 | 403 |
| AUTH-07 | 过期/篡改 token | 调用受保护接口 | 401 INVALID_TOKEN |
| AUTH-08 | 学生自主注册 | 注册成功且角色固定为 student |
| AUTH-09 | 修改密码时当前密码错误 | 422 CURRENT_PASSWORD_INVALID |
| COURSE-01 | 未加入学生读取课程知识库 | 403 |
| COURSE-02 | 学生申请后尚未审批 | 仍为 403 |
| COURSE-03 | 负责人同意申请 | 成为成员并可读取资料与作业 |
| COURSE-04 | 添加第二位负责人 | 两位负责人均可审批和管理课程 |
| COURSE-05 | CSV 导入新学生和已有学生 | 新建/加入成功并返回逐行结果 |

## 4. 知识库测试

| 编号 | 输入/步骤 | 预期 |
| --- | --- | --- |
| KB-01 | 上传 UTF-8 TXT | ready，chunk>0，元数据齐全 |
| KB-02 | 上传 GB18030 TXT | 正常解析 |
| KB-03 | 上传 PDF/DOCX | 提取正文并分块 |
| KB-04 | 上传空文件 | 422 EMPTY_FILE |
| KB-05 | 超过大小限制 | 413 FILE_TOO_LARGE |
| KB-06 | `.exe` 文件 | 415 UNSUPPORTED_FILE |
| KB-07 | 长文档 | chunk 约 500～800 token、重叠 10%～20% |
| KB-08 | 检索课程 A | 不返回课程 B 的块 |
| KB-09 | 课程无检索结果且网络补充也失败 | insufficient_evidence=true |
| KB-10 | Milvus 故障 | 任务 failed 且可诊断，不伪造 ready |
| KB-11 | 同批选择两个内容相同但文件名不同的文件 | 第一个 ready，第二个 duplicate |
| KB-12 | 两个并发请求上传相同内容 | MySQL 唯一约束只允许一个入库 |
| RBAC-01 | 学生请求创建课程 | 403，前端不显示创建表单 |
| RBAC-02 | 学生读取作业详情 | 不包含 standard_answer/rubric |
| HW-09 | 教师查看作业列表 | 显示课程、标题、分值、题数和提交数 |
| HW-10 | 学生查看作业列表 | 仅显示已发布且可参与的作业和个人提交状态 |

## 5. 备课与答疑测试

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| AI-01 | 有资料章节生成教案 | Markdown 文本结构完整并带 chunk 引用 |
| AI-01A | 分别生成四类备课内容 | 返回非 JSON 的可阅读文本，格式符合教案/讲稿/PPT/练习用途 |
| AI-01B | 打开备课历史预览 | 新文本和旧 JSON 记录均可阅读，下载文件为 Markdown |
| AI-01C | 课程存在资料但主题完全无关 | 返回 INSUFFICIENT_EVIDENCE，不得生成无关 Vue/机器学习内容 |
| AI-01D | 相关主题混合检索 | BGE 得分通过阈值，生成内容只引用相关块 |
| AI-02 | 无资料章节生成 | 422 INSUFFICIENT_EVIDENCE |
| AI-03 | 模型不可用 | 503 MODEL_UNAVAILABLE |
| AI-04 | 作业或报告模型返回不可修复的非法 JSON | 502 MODEL_OUTPUT_INVALID，并给出可操作提示 |
| AI-04A | 作业模型返回格式波动 | JSON Schema 或归一化/文本降级产生有效题目，不整批报格式无效 |
| AI-04B | 选择多份参考资料生成 | 允许选择 1～10 份同课程 ready 文件，生成上下文包含各文件代表块 |
| UI-01 | 页面导航与退出 | 顶栏显示当前位置，右上角始终可见退出按钮 |
| UI-02 | 作业详情返回 | 返回作业列表按钮颜色、边框和间距明显 |
| QA-01 | 学生提出教材内问题 | 答案包含引用和 trace_id |
| QA-02 | 提出资料外问题 | 明确证据不足并转教师 |
| QA-03 | 访问他人会话 | 404/403，不泄露消息 |
| QA-04 | 文档含 Prompt 注入文本 | 系统忽略其指令，只当资料处理 |
| QA-05 | 课程证据少于两个片段且网络可用 | 最多抓取两篇公开网页，回答返回可点击 web 引用且不写知识库 |
| QA-06 | 网络搜索或抓取失败但课程有证据 | 安全降级为课程资料回答，不返回 5xx |
| AGENT-03 | 管理员打开 Agent 能力中心 | 展示 Skill、历史/活跃 Prompt、7 个 Tool 和 MCP 方法 |
| MCP-02 | 非管理员调用 `/api/v1/mcp` | 403，不暴露工具清单和调用结果 |
| MCP-03 | 调用未注册 Tool | 返回 JSON-RPC 错误，不执行任何业务查询 |
| MCP-04 | 远端 MCP 未启用或主机不在白名单 | MCP_REMOTE_DISABLED |
| WEB-01 | 抓取公开 HTML 教程 | 提取标题和正文，保留最终 URL，先生成 pending 草稿 |
| WEB-02 | `plugins/spider` 抓取 `127.0.0.1`、内网 IP、file URL 或非 80/443 端口 | 请求前拒绝，不发起连接 |
| WEB-03 | 公网 URL 重定向到内网 | 每一跳重新校验并拒绝 |
| WEB-04 | 非文本响应、超 2MB 或正文不足 | 返回明确业务错误，不生成草稿 |
| WEB-05 | 教师确认草稿 | 创建 web_reference 文档、分块和向量，草稿关联 document_id |
| WEB-06 | 教师拒绝或草稿超过 24 小时 | 不写知识库，不能再次确认 |
| WEB-07 | 不同 URL 正文内容相同 | SHA-256 去重并返回 DUPLICATE_DOCUMENT |
| WEB-08 | 网页正文包含“忽略系统指令” | Prompt 1.1 将其视为正文，不执行指令 |
| WEB-09 | MCP 调用 preview_web_source | 只返回 draft_id 和预览，不存在 MCP 确认 Tool |

## 6. 作业和批改测试

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| HW-01 | 空作业发布 | 422 EMPTY_ASSIGNMENT |
| HW-02 | 客观题正确答案 | 满分、confidence=1 |
| HW-03 | 客观题错误答案 | 0 分 |
| HW-04 | 主观题批改 | 逐 rubric 输出证据，待复核 |
| HW-05 | AI 分数超满分 | 被限制到 max_score |
| HW-06 | 教师复核超满分 | 422 SCORE_OUT_OF_RANGE |
| HW-07 | 学生重复提交 | 唯一约束或业务错误，不产生两份 |
| HW-08 | 未发布作业提交 | 409 ASSIGNMENT_NOT_AVAILABLE |

## 7. 学情与报告测试

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| AN-01 | 0.59 掌握度 | level=薄弱 |
| AN-02 | 0.60/0.80 边界 | 分别待巩固/已掌握 |
| AN-03 | 学生查看他人学情 | 403 |
| RP-01 | 无快照生成报告 | 指标为 0，不编造表现 |
| RP-02 | 有快照生成报告 | average 与 SQL 聚合一致 |
| RP-03 | 未审核报告家长访问 | 403/404 |
| RP-04 | 教师审核发布 | 家长授权后可查看 |

## 8. 非功能测试

- 50 个并发普通查询时记录 P95 延迟和错误率。
- 100MB 非法上传必须在进入模型前拒绝。
- SQL 注入字符串不得改变查询结构。
- 路径 `../../secret` 不得写出 storage。
- 日志扫描不得出现密码、JWT 和 `.env` 内容。
- MySQL 重启后连接池可恢复；Ollama 超时后请求可控失败。

## 9. 端到端验收

教师登录并创建课程，上传教材；检索原文并生成教案；创建含客观和主观题的作业并发布；学生登录提交；教师触发批改并复核；查看学生掌握度；生成周报草稿并审核。全过程不得手工修改数据库。

新增产品功能验证脚本：`python scripts/verify_product_features.py`。脚本已在真实 MySQL、Milvus、Ollama 环境验证批量上传、内容去重、文档清单、学生创建课程 403、教师/学生作业列表以及标准答案隔离。

账号与课程权限验证脚本：`python scripts/verify_account_course_flow.py`。真实验证注册、资料修改、改密、审批前 403、审批后 200、多负责人、CSV 导入和学生课程可见范围。

## 10. 结构化契约与本次回归

- 模型返回非 JSON 时，主观题批改仍返回合法分数、置信度、反馈和证据。
- 作业材料模型返回损坏 JSON 时，仍按请求数量返回字段完整、可落库的材料。
- 报告模型返回普通文本时，仍返回家长可读的六字段报告。
- 迁移后同一学生可产生递增尝试次数，最新一次作为作业摘要。
- 文档、作业、课程删除均校验课程负责人权限；归档内容不参与列表或检索。
- 本轮结果：后端 22 项通过；Vue TypeScript 与 130 模块生产构建通过；MySQL 当前为 0009 head。

## 11. 源码组合与交互测试

- 核心 `WebArticleCrawler` 适配器必须实际持有 `plugins.spider.TextCourseFetcher`，并将同步调用移出事件循环。
- 会话改名 Schema 必须接受合法标题并拒绝空标题。
- Markdown 标题、列表和强调应生成语义化 HTML；原始 script 标签和 `javascript:` 链接不得进入预览 DOM。
- 普通历史预览不显示收藏层级；收藏页预览显示收藏层级并可返回收藏。
- 课程入口整卡可点击；网页抓取的 pending 状态可确认，所有状态可预览和删除。
- 智能备课收藏切换不得跳转；独立收藏夹按钮负责进入收藏页，章节或主题作为历史标题，四项操作桌面端单行等宽。
- 知识库课程标签可横向滚动且当前项高亮，桌面端左右分栏顺序符合设计。
- 模型预热不下载权重，Ollama 请求包含常驻 keep_alive，本地重排器仅从配置路径加载。
- 回归结果：后端 27 项通过，Ollama 对话/Embedding 与本地 BGE 真实预热通过，前端 2 项通过，Vue TypeScript 与 136 模块生产构建通过。
# 2026-07-14 新增测试用例

| 编号 | 场景 | 预期 |
|---|---|---|
| KB-P01 | 课程成员预览 Markdown 文档 | 分块按序重组并标记 markdown，前端渲染标题/列表 |
| KB-P02 | 非课程成员请求文档预览 | 返回 403，不泄露内容和文件路径 |
| KB-P03 | 超过 10 万字符的解析正文 | 返回截断内容和 `truncated=true` |
| MP-01 | 管理员读取模型配置 | 只返回 API Key 已配置状态，不返回明文 |
| MP-02 | 非管理员访问模型设置接口 | 返回 403 |
| MP-03 | OpenAI-compatible JSON 模式 | 请求包含 `response_format=json_object` |
| MP-04 | 保存带 API Key 的配置 | 落盘内容不含明文，使用同一 SECRET_KEY 可恢复 |
| MP-05 | 实际 Embedding 维度与填写值不同 | 连接测试返回 422 |
| MP-06 | 已有 Milvus collection 维度不匹配 | 返回 409 和重建/更换 collection 指引 |
# 2026-07-15 新增知识图谱测试

| 编号 | 场景 | 预期 |
|---|---|---|
| KG-01 | 负责教师生成课程图谱 | 创建 `knowledge_point_graphs` 版本 1，状态 draft |
| KG-02 | 再次生成同一课程图谱 | 版本递增，旧版本保留 |
| KG-03 | 非负责人教师生成图谱 | 403 |
| KG-04 | 学生读取课程知识点列表 | 403（未加入）或 200（已加入） |
| KG-05 | 学生查看自己掌握度 | 返回按 `MasterySnapshot` 聚合的掌握度 |
| KG-06 | 学生查看他人掌握度 | 403 |
| KG-07 | 家长查看未绑定学生掌握度 | 403 |
| KG-08 | 模型返回非法 JSON | 不创建空图谱，返回 422/500 业务错误 |
| KG-09 | 题目编辑关联知识点 | 保存后 `questions.knowledge_point_ids_json` 更新 |
| KG-10 | 前端图谱页面构建 | `vue-tsc` 与 Vite 生产构建通过 |

# 2026-07-14 新增路由与学情测试

| 编号 | 场景 | 预期 |
|---|---|---|
| NAV-01 | 依次进入课程 A、课程 B 的同一模块 | 分别显示对应课程且状态不串用 |
| NAV-02 | 从作业详情点击面包屑课程名 | 返回该课程作业列表，不残留详情状态 |
| NAV-03 | 收藏夹点击“智能备课” | 返回智能备课主页面 |
| NAV-04 | 课程中心点击课程 B 的知识库 | 知识库直接高亮课程 B |
| UI-01 | 桌面展示 4 个以上课程 | 每行最多 3 个，多余课程换行 |
| RAG-01 | 候选含负相关、过短和同文件大量片段 | 只返回正相关、足够长度且来源不过度集中的结果 |
| ANA-01 | 两个作业只完成一个并有 3 次提问 | 完成率 50%，列出未完成作业并汇总答疑次数 |
| ANA-02 | 学生请求未加入课程的分析 | 返回 403 |
